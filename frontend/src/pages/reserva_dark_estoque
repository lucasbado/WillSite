<!-- {/* --- LISTA/TABELA COM ESTÉTICA TECH --- */}
<div className="md:bg-slate-950 md:rounded-[3rem] md:border-4 md:border-slate-900 md:shadow-2xl overflow-hidden">
    
    {/* VISÃO DESKTOP: TABLE TECH */}
    <table className="hidden md:table w-full border-collapse">
        <thead>
            <tr className="text-left bg-slate-900/50 border-b border-slate-800">
                <th className="px-8 py-6 text-[10px] font-black text-blue-500 uppercase tracking-[0.3em] italic">Component_ID / Node</th>
                <th className="px-8 py-6 text-[10px] font-black text-blue-500 uppercase tracking-[0.3em] italic text-center">Units_Available</th>
                <th className="px-8 py-6 text-[10px] font-black text-blue-500 uppercase tracking-[0.3em] italic text-center">Operational_Status</th>
                <th className="px-8 py-6 text-[10px] font-black text-blue-500 uppercase tracking-[0.3em] italic text-right">System_Access</th>
            </tr>
        </thead>
        <tbody className="divide-y divide-slate-900">
            {estoqueFiltrado.map(item => {
                const isLow = item.quantidade <= item.minimo;
                return (
                    <tr key={item.id} className="hover:bg-blue-600/5 transition-all group">
                        <td className="px-8 py-6">
                            <div className="flex items-center gap-6">
                                <div className={`p-4 rounded-2xl border-2 transition-all ${isLow ? 'bg-red-500/10 border-red-500/50 text-red-500 animate-pulse' : 'bg-slate-900 border-slate-800 text-blue-400'}`}>
                                    {getIconByPeca(item.nome)}
                                </div>
                                <div>
                                    <p className="font-black text-slate-100 text-lg uppercase tracking-tighter leading-none mb-2">
                                        {item.nome}
                                    </p>
                                    <div className="flex gap-2">
                                        <span className="text-[9px] font-black bg-blue-600/20 text-blue-400 border border-blue-500/30 px-2 py-0.5 rounded uppercase">
                                            {item.modelo_compativel || 'Universal'}
                                        </span>
                                        <span className="text-[9px] font-black bg-slate-800 text-slate-500 px-2 py-0.5 rounded uppercase font-mono">
                                            {item.categoria || 'Generic'}
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </td>
                        <td className="px-8 py-6">
                            <div className="flex items-center justify-center gap-6 bg-slate-900/50 w-fit mx-auto p-2 rounded-2xl border border-slate-800">
                                <button onClick={() => handleAjustarQtd(item.id, 'subtrai')} className="p-2 text-slate-600 hover:text-red-500 hover:bg-red-500/10 rounded-lg transition-all"><Minus size={16} /></button>
                                <span className={`text-2xl font-black font-mono w-10 text-center ${isLow ? 'text-red-500' : 'text-blue-400'}`}>{item.quantidade}</span>
                                <button onClick={() => handleAjustarQtd(item.id, 'soma')} className="p-2 text-slate-600 hover:text-emerald-500 hover:bg-emerald-500/10 rounded-lg transition-all"><Plus size={16} /></button>
                            </div>
                        </td>
                        <td className="px-8 py-6 text-center">
                            <div className={`inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-[9px] font-black uppercase tracking-widest border-2 ${isLow ? 'bg-red-500/10 border-red-500/50 text-red-500' : 'bg-emerald-500/10 border-emerald-500/50 text-emerald-500'}`}>
                                <div className={`w-1.5 h-1.5 rounded-full ${isLow ? 'bg-red-500 animate-ping' : 'bg-emerald-500'}`}></div>
                                {isLow ? 'Critical_Supply' : 'Stock_Optimal'}
                            </div>
                        </td>
                        <td className="px-8 py-6 text-right">
                            <div className="flex justify-end gap-3">
                                <button onClick={() => abrirModalEdicao(item)} className="p-3 bg-slate-900 text-slate-500 hover:text-blue-400 border border-slate-800 rounded-xl transition-all"><Edit3 size={18} /></button>
                                <button onClick={() => handleDeletar(item.id)} className="p-3 bg-slate-900 text-slate-500 hover:text-red-500 border border-slate-800 rounded-xl transition-all"><Trash2 size={18} /></button>
                            </div>
                        </td>
                    </tr>
                );
            })}
        </tbody>
    </table>

    {/* VISÃO MOBILE: COMMAND MODULES */}
    <div className="md:hidden space-y-4">
        {estoqueFiltrado.map(item => {
            const isLow = item.quantidade <= item.minimo;
            return (
                <div key={item.id} className={`bg-slate-950 p-6 rounded-[2.5rem] border-4 transition-all shadow-xl ${isLow ? 'border-red-600/50 shadow-red-900/20' : 'border-slate-900 shadow-blue-900/10'}`}>
                    <div className="flex items-start justify-between mb-6">
                        <div className="flex items-center gap-4">
                            <div className={`p-4 rounded-2xl border-2 ${isLow ? 'bg-red-500/10 border-red-500/50 text-red-500' : 'bg-slate-900 border-slate-800 text-blue-400'}`}>
                                {getIconByPeca(item.nome)}
                            </div>
                            <div>
                                <h4 className="font-black text-slate-100 uppercase tracking-tighter italic text-lg leading-none mb-1">{item.nome}</h4>
                                <p className="text-[9px] font-bold text-slate-500 uppercase tracking-widest">{item.modelo_compativel || 'Universal_Node'}</p>
                            </div>
                        </div>
                        <div className="flex flex-col gap-2">
                            <button onClick={() => abrirModalEdicao(item)} className="p-2 bg-slate-900 border border-slate-800 rounded-lg text-slate-500"><Edit3 size={16} /></button>
                        </div>
                    </div>

                    {/* INTERFACE DE CONTROLE QUANTIDADE (JOYSTICK TECH) */}
                    <div className="grid grid-cols-3 items-center bg-slate-900 rounded-2xl border-2 border-slate-800 overflow-hidden">
                        <button 
                            onClick={() => handleAjustarQtd(item.id, 'subtrai')}
                            className="h-16 flex items-center justify-center bg-slate-900 hover:bg-red-500/10 text-slate-500 active:text-red-500 transition-all border-r border-slate-800"
                        >
                            <Minus size={24} strokeWidth={3} />
                        </button>
                        
                        <div className="flex flex-col items-center justify-center">
                            <span className="text-[8px] font-black text-blue-500/50 uppercase tracking-widest mb-1">Stock_Val</span>
                            <span className={`text-2xl font-mono font-black ${isLow ? 'text-red-500 animate-pulse' : 'text-blue-400'}`}>
                                {item.quantidade}
                            </span>
                        </div>

                        <button 
                            onClick={() => handleAjustarQtd(item.id, 'soma')}
                            className="h-16 flex items-center justify-center bg-slate-900 hover:bg-emerald-500/10 text-slate-500 active:text-emerald-500 transition-all border-l border-slate-800"
                        >
                            <Plus size={24} strokeWidth={3} />
                        </button>
                    </div>
                    
                    {isLow && (
                        <div className="mt-4 py-2 bg-red-600/10 border border-red-600/30 rounded-xl flex items-center justify-center gap-2">
                            <AlertCircle size={12} className="text-red-500" />
                            <span className="text-[9px] font-black text-red-500 uppercase italic animate-pulse">Critical: Resupply Required</span>
                        </div>
                    )}
                </div>
            );
        })}
    </div>
</div> -->